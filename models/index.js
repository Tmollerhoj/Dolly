const User = require('./User');
const Follower = require('./Follower');
const Comment = require('./Comment');
const Bleet = require('./Bleet');

// TODO: reference foreign keys

module.exports = { User, Follower, Comment, Bleet };

